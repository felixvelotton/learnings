main() {
  const lista = ['Ana', 'Lia', 'Gui'];
  //lista.add('rebeca'); quando definimos nossa lista como const não é
  //possivel modificar a lista, porem se definimos como var é possivel
  print(lista);

  ///lista = ['Banana', 'Maça'];
  //print(lista);
}
